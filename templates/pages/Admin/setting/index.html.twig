{% extends 'themes/base_admin.html.twig' %}

{% block title %}Les paramèttres du site{% endblock %}

{% block description %}Les paramèttres du site{% endblock %}


{% block main %}
    <h1 class="text-center my-3 display-5">Les paramèttres du site</h1>

    {# {% for message in app.flashes('success') %}
        <div class="text-center alert alert-success alert-dismissible fade show" role="alert">
        {{message}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        
    {% endfor %}

     {% for message in app.flashes('warning') %}
        <div class="text-center alert alert-warning alert-dismissible fade show" role="alert">
        {{message}}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        
    {% endfor %} #}

      {% include 'components/alert.html.twig' %}   

   <div class="table-responsive">
        <table id="table" class="table table-striped table-hover table-bordered text-center">
            <thead class="table-dark">
    
                <tr>
                    
                    <th>Email</th>
                    <th>Téléphone</th>
                    <th>Créé le</th>
                    <th>Modifié le</th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
               
                <tr>
                   
                    <td>{{setting.email}}</td>
                    <td>{{setting.phone ? setting.phone : '==='}}</td>
                    <td>{{setting.createdAt|format_datetime()}}</td>
                    <td>{{setting.updatedAt|format_datetime()}}</td>
                    <td>

                           <a class="mx-2 text-secondary" href="{{path('admin_setting_edit',{'id':setting. id})}}"><i class="fas fa-edit"></i></a>
                        </form>
                    </td>
                </tr>
                
            </tbody>
        </table>
    </div>
 {% endblock %} 
    